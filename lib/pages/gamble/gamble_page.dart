import 'package:flutter/material.dart';

class GamblePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Gamble page."),
    );
  }
}
